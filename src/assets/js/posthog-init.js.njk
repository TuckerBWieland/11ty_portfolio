---
permalink: "/assets/js/posthog-init.js"
---
(function(){
  if (window.posthog && window.posthog.__LOADED__) return;

  // Respect Do Not Track
  try {
    var dnt = (navigator.doNotTrack == "1") || (window.doNotTrack == "1") || (navigator.msDoNotTrack == "1");
    if (dnt) { window.posthog = { capture:function(){}, init:function(){}, __DNT__:true }; return; }
  } catch(_) {}

  // Lightweight loader (served from our proxy)
  !function(t,e){var o,n,p,r;e.__SV||(window.posthog=e,e._i=[],e.init=function(i,s,a){
    function g(t,e){var o=e.split(".");2==o.length&&(t=t[o[0]],e=o[1]),t[e]=function(){
      t.push([e].concat(Array.prototype.slice.call(arguments,0)))
    }};
    (p=t.createElement("script")).type="text/javascript"; p.async=!0;
    p.src=s.api_host + "/static/array.js"; // <- from our proxy
    (r=t.getElementsByTagName("script")[0]).parentNode.insertBefore(p,r);
    var u=e; void 0!==a ? u=e[a]=[] : a="posthog";
    u.people=u.people||[];
    u.toString=function(t){var e="posthog";return "posthog"!==a && (e+="."+a), t||(e+=" (stub)"), e};
    u.people.toString=function(){return u.toString(1)+".people (stub)"};
    o="capture identify alias people.set people.set_once set_config register register_once unregister opt_out_capturing has_opted_out_capturing opt_in_capturing reset isFeatureEnabled onFeatureFlags reloadFeatureFlags group groupIdentify getGroupPeople getGroups setPersonPropertiesOnce".split(" ");
    for(n=0;n<o.length;n++) g(u,o[n]);
    e._i.push([i,s,a]);
  },e.__SV=1)}(document,window.posthog||[]);

  // Only render in production when the key is present
  {% if (process.env.ELEVENTY_ENV == "production" or process.env.NODE_ENV == "production") and process.env.POSTHOG_KEY %}
  posthog.init('{{ process.env.POSTHOG_KEY | safe }}', {
    api_host: 'https://logs.tuckerwieland.com',
    ui_host: 'https://us.posthog.com',
    persistence: 'localStorage+cookie',
    capture_pageview: true
  });
  window.posthog.__LOADED__ = true;
  console.log("✅ PostHog loaded via proxy");
  {% endif %}
})();
